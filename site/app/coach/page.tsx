import { CoachDashboard } from '../../components/CoachDashboard';

export const metadata = {
  title: 'Coach workspace — ParentConnect AI',
  description:
    'A private coach workspace: keep your chats, start new ones, in Kinyarwanda or English. Conversations stay on your device only.',
};

export default function CoachPage() {
  return <CoachDashboard />;
}
