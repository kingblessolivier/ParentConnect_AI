{
  "name": "@parentconnect/site",
  "version": "0.0.0",
  "private": true,
  "description": "ParentConnect AI public site — what the programme is, how to reach it (app/SMS/community sessions), and always-visible safeguarding help. Not the staff console (see ../web) and not the parent app (see ../mobile).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "next dev -p 3003",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "lucide-react": "^1.28.0",
    "next": "^14.2.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.0",
    "typescript": "^5.4.0"
  }
}
